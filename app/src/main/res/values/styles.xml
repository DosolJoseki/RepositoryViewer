<resources xmlns:android="http://schemas.android.com/tools">

    <!-- Base application theme. style/Theme.AppCompat.Light.NoActionBar   Base.Theme.AppCompat.Light.DarkActionBar-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <style name="ElementTheme" parent="@android:style/Theme.Light">
        <item name="android:background">@color/colorElement</item>
        <item name="android:elevation">3dp</item>
    </style>

</resources>
